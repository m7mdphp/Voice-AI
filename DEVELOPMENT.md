# دليل التطوير والتشغيل - نظام ترياق الصوتي (Tiryaq Dev Guide)

## 1. المقدمة (Introduction)
أهلاً بك في بيئة تطوير **نظام ترياق**، المساعد الصوتي السعودي الذكي.
تم تصميم هذا النظام ليكون:
- **سريع الاستجابة (Real-time):** باستخدام WebSockets و WebRTC.
- **معدد المستأجرين (Multi-tenant):** لخدمة عدة عملاء ببيانات معزولة.
- **عالي الجودة:** يدعم اللهجة السعودية البيضاء حصراً.

---

## 2. المتطلبات الأساسية (Prerequisites)
قبل البدء، تأكد من تثبيت الأدوات التالية على جهازك:
1.  **Docker Desktop:** لضمان تشغيل النظام في حاويات معزولة.
2.  **Git:** لإدارة النسخ.
3.  **محرر أكواد:** (يفضل VS Code).

---

## 3. إعداد المفاتيح والأسرار (Secrets Setup)

### أ. ملف اعتماد Google Cloud
1.  قم بتحميل ملف `serviceAccountKey.json` من لوحة تحكم Firebase/Google Cloud.
2.  ضع الملف في المجلد الرئيسي للمشروع أو داخل مجلد `backend/`.
3.  **تنبيه هام:** تأكد من أن هذا الملف موجود في `.gitignore` ولا ترفعه أبداً للمستودع.

### ب. متغيرات البيئة (.env)
1.  توجه إلى مجلد `backend/`.
2.  انسخ ملف المثال:
    ```bash
    cp .env.example .env
    ```
3.  افتح ملف `.env` وقم بتعبئة القيم الحقيقية (مثل `GEMINI_API_KEY` و `FIRESTORE_PROJECT_ID`).

---

## 4. التشغيل (Deployment)

لتشغيل النظام بالكامل (الواجهة الخلفية + الواجهة الأمامية) بضغطة زر واحدة:

1.  افتح الطرفية (Terminal) في المجلد الرئيسي للمشروع.
2.  نفذ الأمر التالي لبناء وتشغيل الحاويات:
    ```bash
    docker-compose up --build
    ```
3.  انتظر حتى تظهر رسالة: `Application startup complete`.

### الوصول للنظام:
- **الواجهة الأمامية (Frontend):** [http://localhost:5173](http://localhost:5173)
- **الواجهة الخلفية (Backend API):** [http://localhost:8000/docs](http://localhost:8000/docs) (Swagger UI)

---

## 5. اختبار الاتصال (Health Check)

للتأكد من أن الـ WebSocket يعمل بشكل صحيح:
1.  افتح الواجهة الأمامية في المتصفح.
2.  اضغط على زر **"بدء المحادثة 🎙️"**.
3.  يجب أن تتغير الحالة إلى **"أسمعك.."** (Listening).
4.  تحدث في الميكروفون؛ إذا رأيت رسالة رد من "ترياق"، فالاتصال سليم.

يمكنك أيضاً استخدام أداة مثل `Postman` أو `wscat` للاختبار المباشر:
```bash
wscat -c ws://localhost:8000/ws/session/test_tenant/user_1
```

---

## 6. ملاحظة أمنية (Security Warning) 🚨

> [!CAUTION]
> **لا تقم مطلقاً برفع الملفات التالية إلى GitHub أو أي مستودع عام:**
> *   `serviceAccountKey.json`
> *   `backend/.env`
> *   أي ملف يحتوي على مفاتيح API حقيقية.
>
> تم إعداد ملف `.gitignore` لحماية هذه الملفات، ولكن وجب التنويه لخطورة الأمر.

---

**فريق تطوير ترياق** - 2026
